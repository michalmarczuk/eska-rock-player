export interface ISongs {
    current: {
        image: string;
        artist: string;
        song: string;
    },
    last: {
        image: string;
        artist: string;
        song: string;
    }
}